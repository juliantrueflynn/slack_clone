import { call, putt, takeEvery, takeLatest } from 'redux-saga/effects';
import {
  RECEIVE_WORKSPACES,
  RECEIVE_WORKSPACE
} from '../actions/workspace_actions';

function* rootSagas() {
}

export default rootSagas;